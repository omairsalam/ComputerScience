For the project, you will write another EventList class that uses the Skip List data structure described in
class and in section 3.5 of Goodrich and Tamassia. The easiest way to deal with multiple classes with the
same set of operations is to have them all implement an interface. In the Content > Assignments section
on Blackboard, you will find a file called EventListInterface.java — drop this file in with your source
code and then have all of your event list classes implement the interface. Call the new class that you write
EventSkipList. This interface specifies the following operations: